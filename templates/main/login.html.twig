{% extends 'base.html.twig' %}
{% block title %}Login - GymForce{% endblock %}

{% block body %}
<style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@500;700&display=swap');

    body {
        font-family: 'Inter', sans-serif;
        background: linear-gradient(135deg, #111, #1c1c1c);
        position: relative;
        overflow-x: hidden;
    }

    .login-wrapper {
        position: relative;
        z-index: 1;
    }

    .blur-bg {
        position: absolute;
        width: 600px;
        height: 600px;
        background: #e63946;
        opacity: 0.08;
        border-radius: 50%;
        top: -150px;
        left: -100px;
        filter: blur(120px);
        z-index: 0;
    }

    .login-card {
        background: rgba(40, 40, 40, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 16px;
        box-shadow: 0 15px 35px rgba(230, 57, 70, 0.2);
        padding: 2.5rem;
        max-width: 420px;
        width: 100%;
        margin: auto;
        animation: slideFade 0.5s ease-in-out;
        color: #fff;
    }

    @keyframes slideFade {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .login-card h2 {
        color: #e63946;
        font-weight: 700;
        margin-bottom: 1.5rem;
        text-align: center;
    }

    .form-label {
        font-weight: 600;
        color: #ccc;
    }

    .form-control {
        border-radius: 10px;
        padding: 10px 15px;
        background-color: #2b2b2b;
        border: 1px solid #444;
        color: #fff;
    }

    .form-control::placeholder {
        color: #aaa;
    }

    .form-control:focus {
        border-color: #e63946;
        box-shadow: 0 0 0 0.2rem rgba(230, 57, 70, 0.25);
        background-color: #2b2b2b;
        color: #fff;
    }

    .btn-primary {
        background-color: #e63946;
        border: none;
        border-radius: 12px;
        font-weight: 600;
        padding: 0.75rem;
        transition: background-color 0.3s ease;
    }

    .btn-primary:hover {
        background-color: #ff4d5a;
    }

    .register-link a {
        color: #e63946;
        font-weight: 500;
        text-decoration: none;
    }

    .register-link a:hover {
        text-decoration: underline;
    }

    .alert {
        border-radius: 0.75rem;
        font-size: 0.9rem;
    }
</style>

<div class="blur-bg"></div>
<div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="login-card">
        <h2>Login to GymForce</h2>

        {% if app.flashes('error') %}
            {% for message in app.flashes('error') %}
                <div class="alert alert-danger">{{ message }}</div>
            {% endfor %}
        {% endif %}

        <form method="post" action="{{ path('login') }}">
            <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input 
                    type="text" 
                    id="email" 
                    name="email" 
                    class="form-control" 
                    value="{{ app.flashes('email')|first }}" 
                    required 
                    autofocus
                    placeholder="you@example.com"
                >
            </div>

            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input 
                    type="password" 
                    id="password" 
                    name="password" 
                    class="form-control" 
                    required
                    placeholder="••••••••"
                >
            </div>

            <div class="d-grid mb-3">
                <button type="submit" class="btn btn-primary">Login</button>
            </div>

            <div class="text-center register-link">
                <small>Don't have an account? <a href="{{ path('signup') }}">Register here</a></small>
            </div>
        </form>
    </div>
</div>
{% endblock %}
