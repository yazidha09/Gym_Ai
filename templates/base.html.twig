<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}GymForce{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(to right, #1c1c1c, #2e2e2e);
            color: #f1f1f1;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .navbar {
            background-color: #000;
            padding: 15px 0;
        }

        .navbar-brand {
            font-size: 1.8rem;
            font-weight: bold;
            color: #e63946 !important;
            letter-spacing: 2px;
        }

        .navbar-nav .nav-link {
            color: #ffffff !important;
            font-weight: 500;
            text-transform: uppercase;
            margin-left: 15px;
            transition: color 0.3s ease;
        }

        .navbar-nav .nav-link:hover {
            color: #e63946 !important;
        }

        .card {
            border: none;
            border-radius: 1rem;
            background-color: #2b2b2b;
            color: white;
            transition: transform 0.2s ease-in-out, box-shadow 0.2s;
        }

        .card:hover {
            transform: scale(1.03);
            box-shadow: 0 0.5rem 1rem rgba(230, 57, 70, 0.4);
        }

        .btn-primary, .btn-success {
            border-radius: 30px;
            background-color: #e63946;
            border: none;
        }

        .btn-primary:hover, .btn-success:hover {
            background-color: #ff4d5a;
        }

        h1, h2, h5 {
            font-weight: 700;
            color: #fff;
        }

        .footer {
            background-color: #111;
            color: #aaa;
            padding: 20px 0;
            text-align: center;
        }
    </style>
</head>
<body>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('home') }}">
            <i class="bi bi-barbell"></i> GymForce
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="{{ path('home') }}">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('login') }}">Login</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('ai') }}">AI Nutrition</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('signup') }}">Join Us</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('about') }}">About</a></li>
            </ul>
        </div>
    </div>
</nav>

{% block body %}{% endblock %}

<!-- Footer -->
<div class="footer mt-5">
    <div class="container">
        <p>&copy; {{ "now"|date("Y") }} GymForce - Stronger Every Day</p>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
